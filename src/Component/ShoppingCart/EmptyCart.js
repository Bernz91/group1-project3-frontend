import React from "react";
import Typography from "@mui/material/Typography";

const EmptyCart = () => {
  return <Typography variant="h6">Uh oh! There is no orders yet! </Typography>;
};

export default EmptyCart;
