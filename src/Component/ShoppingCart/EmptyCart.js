import React from "react";
import Typography from "@mui/material/Typography";

const EmptyCart = () => {
  return <Typography variant="h4">Uh oh! There is no orders here. </Typography>;
};

export default EmptyCart;
